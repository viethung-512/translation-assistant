import { RealtimeToken } from "@soniox/client";
import { FC } from "react";

type TokenBlockProps = {
  tokens: RealtimeToken[];
};

export const TokenBlock: FC<TokenBlockProps> = () => {
  return <></>;
};
